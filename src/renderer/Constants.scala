package renderer

object Constants {
  // Distances from camera
  val NearPlane = 0.1
  val FarPlane = 30.0
  
  val MovementSpeed = 0.02
  val RotationSpeed = 0.01
  
  // Used when comparing two doubles
  val DoubleEps = 1e-9
  
  // Dimensions of the program window, in pixels
  val WindowWidth = 1024
  val WindowHeight = 768
}
